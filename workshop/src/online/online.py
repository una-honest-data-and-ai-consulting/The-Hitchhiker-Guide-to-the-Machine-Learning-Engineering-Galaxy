class OnlineModel(object):

    def __init__(self, model):
        self.model = model

    def load(self):
        pass

    def predict(self, input_features):
        '''
        Accepts input features and returns a prediction.

        Parameters:
        input_features: Features required by the model to generate a
        prediction. For example: numpy arrays of a particular dimension

        Returns:
        prediction: Prediction of the model. For example: a numpy array
        '''
        pass
